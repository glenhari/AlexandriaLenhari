@model IEnumerable<Alexandria.Models.Exemplar>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_MyLayout.cshtml";
}

<h2 class="title1">Index</h2>

<p>
    @Html.ActionLink("Adicionar", "Create")
</p>
<div class="grid-bottom widget-shadow">
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Livro.LivroTitulo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ExemplarSerial)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Livro.LivroTitulo)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ExemplarSerial)
                </td>
                <td>
                    @Html.ActionLink("Editar", "Edit", new { id = item.ExemplarId }) |
                    @Html.ActionLink("Detalhes", "Details", new { id = item.ExemplarId }) |
                    @Html.ActionLink("Apagar", "Delete", new { id = item.ExemplarId })
                </td>
            </tr>
        }

    </table>
</div>
